# 280. Sort With 2 Stacks

```java
public class Solution {
  public void sort(LinkedList<Integer> s1) {
    LinkedList<Integer> s2 = new LinkedList<Integer>();
    //move all the elements from s1 to s2
    while(!s1.isEmpty()){
      s2.offerFirst(s1.pollFirst());
    }
    while(!s2.isEmpty()){
      int cur=s2.pollFirst();
      while(!s1.isEmpty()&&s1.peekFirst()<cur){//这里必须是while而不是if,因为可以保证第一个s2过来之后，下一个s1还是小于cur
        s2.offerFirst(s1.pollFirst());
      }
      s1.offerFirst(cur);
    }
  }
}

import com.fasterxml.jackson.databind.ObjectMapper;
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.util.HashMap;
import java.util.Map;

public class GetCertainPaystubHelper extends BaseHelper {

    public APIResponse getCurrentPaystubResponse(String consumerId, String serviceName, String serviceEnv) throws Throwable {

        // 构造 GraphQL 查询字符串
        String query = "{ getCertainPayStub(win: \"232292196\", payrollRunDate: \"2024-10-08\", "
                     + "checkType: \"Weekly\", checkDate: \"2024-10-11\", "
                     + "checkNbr: \"666666012012641449\") {"
                     + "   id"
                     + "   payAssociateInfo { userID firstName lastName }"
                     + "   payPeriodSummary { win year }"
                     + " }"
                     + "}";

        // 创建请求体，将 query 放入其中
        Map<String, Object> body = new HashMap<>();
        body.put("query", query);
        body.put("variables", null);  // 如果没有变量则传 null

        // 将请求体转换为 JSON 字符串
        ObjectMapper objectMapper = new ObjectMapper();
        String requestBody = objectMapper.writeValueAsString(body);

        // 设置请求头信息
        Map<String, String> headers = new HashMap<>();
        headers.put("Content-Type", "application/json");
        headers.put("WM_CONSUMER.ID", consumerId);
        headers.put("WM_SVC.ENV", serviceEnv);
        headers.put("WM_SVC.NAME", serviceName);
        headers.put("Authorization", "Bearer eyJhbGciOi...");  // 替换为你的 Token

        // 创建 HTTP 请求
        HttpRequest.Builder requestBuilder = HttpRequest.newBuilder()
                .uri(URI.create("https://paystub-api-global-us.stage.walmart.com/graphql"))
                .POST(HttpRequest.BodyPublishers.ofString(requestBody));

        // 添加请求头
        headers.forEach(requestBuilder::header);

        // 发送请求并获取响应
        HttpClient httpClient = HttpClient.newHttpClient();
        HttpRequest request = requestBuilder.build();
        HttpResponse<String> response = httpClient.send(request, HttpResponse.BodyHandlers.ofString());

        // 返回 API 响应
        return new APIResponse(response.body());
    }
}
